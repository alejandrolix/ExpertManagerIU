import { NumerosDecimalesDirective } from './numeros-decimales.directive';

describe('SoloNumerosDirective', () => {
  it('should create an instance', () => {
    const directive = new NumerosDecimalesDirective();
    expect(directive).toBeTruthy();
  });
});
