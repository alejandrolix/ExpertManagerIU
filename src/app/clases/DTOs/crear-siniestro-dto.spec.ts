import { CrearSiniestroDto } from './crear-siniestro-dto';

describe('CrearSiniestroDto', () => {
  it('should create an instance', () => {
    expect(new CrearSiniestroDto()).toBeTruthy();
  });
});
